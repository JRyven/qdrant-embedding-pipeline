# Mock Data

This directory contains safe sample Markdown and generated fixture files for local testing.

The sample documents are intentionally generic and should not be replaced with private project data. Generated smoke-test outputs belong under `data/` and should remain untracked.
